<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo
       http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <dubbo:application name="${dubbo.application.name}"/>
    <dubbo:registry server="zkRegistry" protocol="${dubbo.registry.protocol}" address="${dubbo.registry.address}" file="${dubbo.registry.file}"/>
    <dubbo:protocol name="dubbo" port="${dubbo.protocol.port}"/>
    <dubbo:provider timeout="${dubbo.provider.timeout}"/>
    <dubbo:consumer check="false" timeout="${dubbo.consumer.timeout}"/>

    <dubbo:service interface="com.footmanff.dubbo.SomeApi" ref="someApi"/>

    <dubbo:reference id="fooService" interface="com.weidai.cf.third.api.huadao.HuadaoApi">
        <dubbo:method name="credit" async="true"/>
    </dubbo:reference>

</beans>